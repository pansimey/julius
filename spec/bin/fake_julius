#!/usr/bin/env ruby

$:.unshift(File.expand_path(File.dirname(__FILE__)) + '/../lib')

require 'daemons'
require 'fake_julius'

fake_julius = FakeJulius.new

Daemons.run_proc('fake_julius') do
  fake_julius.start
end
